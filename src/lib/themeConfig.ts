// ./themeConfig.ts
import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#127FFF',
    fontFamily: 'Roboto',
  },
};

export default theme;
